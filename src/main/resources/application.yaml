spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quanlythanhvien
    username: root
    password: example
#    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
  servlet:
    context-path: /quanlythanhvien
